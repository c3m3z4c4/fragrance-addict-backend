# Puerto del servidor
PORT=3000

# Entorno (development o production)
NODE_ENV=production

# API Key (legacy — para /metrics endpoint)
# Genera con: openssl rand -hex 32
API_KEY=tu-api-key-secreta-aqui

# ─── JWT / Auth ────────────────────────────────────────────────────────────────
# Genera con: openssl rand -hex 32
JWT_SECRET=genera_con_openssl_rand_-hex_32
JWT_EXPIRES_IN=7d

# Superadmin inicial — se crea automáticamente al iniciar si no existe
ADMIN_EMAIL=tu@email.com
ADMIN_PASSWORD=contraseña_segura

# ─── Google OAuth ──────────────────────────────────────────────────────────────
# 1. Ve a Google Cloud Console → APIs & Services → Credentials
# 2. Crea un OAuth 2.0 Client ID (Web application)
# 3. Authorized redirect URI: https://tu-backend.com/api/auth/google/callback
GOOGLE_CLIENT_ID=xxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=xxxx
GOOGLE_CALLBACK_URL=https://tu-backend.com/api/auth/google/callback
FRONTEND_URL=https://tu-frontend.com

# Base de datos PostgreSQL
# En Dokploy usar el connection string que te proporciona el servicio de DB
# Formato: postgresql://usuario:password@host:puerto/database
DATABASE_URL=postgresql://perfumes_user:tu_password@db:5432/perfumes

# Configuración de scraping
SCRAPE_DELAY_MS=3000
MAX_CONCURRENT_SCRAPES=2

# CORS - pon true para permitir todos los orígenes (recomendado para empezar)
CORS_ALLOW_ALL=true

# CORS - dominios específicos (separados por coma) — ignorado si CORS_ALLOW_ALL=true
ALLOWED_ORIGINS=https://tu-frontend.tudominio.com

# ─── Gemini AI ─────────────────────────────────────────────────────────────────
# Necesario para el feature de recomendaciones IA (usuarios con cuenta Gmail/Google)
# Obtén tu clave en: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=tu-gemini-api-key-aqui

# Puppeteer — no tocar, el Dockerfile ya lo configura
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
